version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install
        - npm ci --quiet
    build:
      commands:
        - npm run build
        - npm test
  artifacts:
    baseDirectory: dist/ezmc-management-console
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
